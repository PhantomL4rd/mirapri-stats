{
  "name": "mirapuri-stats",
  "version": "0.0.1",
  "private": true,
  "description": "FF14 glamour combination statistics tool",
  "type": "module",
  "scripts": {
    "dev:scraper": "pnpm --filter @mirapuri/scraper dev",
    "build:scraper": "pnpm --filter @mirapuri/scraper build",
    "test": "pnpm -r test",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "typecheck": "pnpm -r typecheck",
    "migrate:writer": "pnpm -F @mirapuri/writer exec wrangler d1 migrations apply mirapuri-stats --remote",
    "db:generate": "pnpm -F @mirapuri/shared run db:generate",
    "db:push": "pnpm -F @mirapuri/shared run db:push"
  },
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=9.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.11"
  }
}
